import React from "react"

import classes from "./Pokus.module.css";

export const Pokus = () => (
    <div className={classes.neco}>
        Muj pokus
    </div>
);

Pokus.propTypes = {
};

Pokus.defaultProps = {
};
